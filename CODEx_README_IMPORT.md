# Codex import source

The real prototype source files are available to the user in the ChatGPT conversation as `le-canton-prototype-lab.zip` and individual HTML uploads. Codex could not access those previous ChatGPT uploads directly.

Required final structure:

```text
index.html
assets/styles.css
prototypes/grande-usine.html
prototypes/mission-tri.html
prototypes/nirvana-engine-v2.html
prototypes/nirvanaz-engine-v2.html
package.json
vercel.json
```

Important:
- Do not create placeholders.
- Do not simplify the prototypes.
- Use the real uploaded HTML files only.
- Verify every link before deployment.

If the real ZIP or HTML files are not available inside the Codex task, ask the user to attach `le-canton-prototype-lab.zip` directly to the Codex task, then extract and copy the files.
